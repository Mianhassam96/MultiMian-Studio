# This workflow is DISABLED. Deployment is handled by nextjs.yml.
# Keeping this file to avoid breaking git history but the `on` trigger is removed.
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled. Use nextjs.yml instead.'
        required: false
